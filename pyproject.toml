[project]
name = "annotation_methods"
version = "0.1.0"
description = "Utility package for Thesis WDV experiments"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
